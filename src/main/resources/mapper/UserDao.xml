<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.bodiary.user.dao.UserDao">
	<select id="getUser" resultType="kr.or.bodiary.user.dto.UserDto">
		select *
		from User
		where user_email= #{user_email}
	</select>
	<insert id="insertUser">
		INSERT INTO User(
			user_email, user_pwd, user_nickname, user_gender, user_age , user_height, user_weight, user_img, user_reportedcount, enabled
			) 
		VALUES( #{user_email},#{user_pwd}, #{user_nickname} ,#{user_gender},#{user_age}, 0, 0,'user.png', 0, 1)
	</insert>
	<update id="updateUser">
		update User
		set user_age = #{user_age}, user_height=#{user_height}, user_weight=#{user_weight}, user_gender=#{user_gender}, user_img=#{user_img}
		where user_email = #{user_email}
	</update>
	<!-- 회원가입시 이메일 체크 -->
	<select id="emailCheck" resultType="int">
		select count(*)
		from User
		where user_email=#{user_email}
	</select>
	
</mapper>