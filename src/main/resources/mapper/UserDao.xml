<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.bodiary.user.dao.UserDao">
	<select id="getUser" parameterType="String" resultType="kr.or.bodiary.user.dto.UserDto">
		select *
		from User
		where user_email= #{user_email}
	</select>
	<insert id="insertUser">
		INSERT INTO User(
			user_email, user_pwd, user_nickname, user_gender, user_age , user_height, user_weight, user_reportedcount, enabled
			) 
		VALUES( #{user_email},#{user_pwd}, #{user_nickname} ,'선택안함',19000101, 0, 0, 0, 1)
	</insert>
	<!-- <update id="updateMember">
		update member
		set pwd = #{pwd}, name=#{name}, cphone=#{cphone}, email=#{email}
		where userid = #{userid}
	</update> -->
	<!-- 회원가입시 이메일 체크 -->
	<select id="emailCheck" resultType="int">
		select count(*)
		from User
		where user_email=#{user_email}
	</select>
	
</mapper>