<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.or.bodiary.routineBrd.dao.RoutineBrdDao">

	<!-- 리스트 -->
    <select id="routineBoardList" resultType="kr.or.bodiary.routineBrd.dto.RoutineBrdDto">
		select user_nickname, routine_brd_title, brd_rec
		  from RoutineBoard
	</select>
	
	<!-- 상세 -->
	<select id="routineBoardSelect" parameterType="int" resultType="kr.or.bodiary.routineBrd.dto.RoutineBrdDto">
		select routine_brd_seq, user_nickname, routine_brd_title, routine_brd_content, routine_cart_seq, brd_image1, brd_image2
		  from RoutineBoard where routine_brd_seq = #{routine_brd_seq}
	</select>
	
	<!-- 수정 -->
	<update id="routineBoardUpdate" parameterType="kr.or.bodiary.routineBrd.dto.RoutineBrdDto">
		update routineboard
		   set routine_brd_seq=#{routine_brd_seq}, routine_brd_title=#{routine_brd_title}, routine_brd_content=#{routine_brd_content}
		 where routine_brd_seq=#{routine_brd_seq}
	</update>
	
	<!-- 삭제 -->
	<delete id="routineBoardDelete" parameterType="int">
		delete
		  from routineboard
		 where routine_brd_seq=#{routine_brd_seq}
	</delete>
	
</mapper>
